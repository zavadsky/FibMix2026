Program encodes word-based text or integer sequences using Elias, Fib3, Mised digit Fibonacci and BCMix codes. Each integer is represented as 4 bytes.
Usage:
Word-based text: FibMix.exe input_file.name
Integer sequence: FibMix.exe input_file.name int32
